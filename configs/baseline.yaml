experiment:
  name: baseline_lr
  output_dir: results

data:
  n_samples: 2000
  n_features: 20
  n_informative: 10
  n_redundant: 5
  class_sep: 1.0
  flip_y: 0.01

model:
  type: logistic_regression
  params:
    C: 1.0
    max_iter: 1000

cv:
  folds: 5
  shuffle: true

run:
  seed: 42
